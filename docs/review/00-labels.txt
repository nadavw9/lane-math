Lane Math — Phase 3 renderer review
Playable gate: load, tap, win, fail, restart. Placeholder rects, system font, no juice.
Mode hardcoded to Normal. Design surface 420x900.

01-initial-1-01.png
    Level 1-01 on load. FULL target queue visible from open (GDD 4.2) — 14, 5, 15
    stacked bottom-up with the FRONT target (14) at the bottom, gold outline.
    Empty equation row, operator row showing Normal budgets (+ x3, - x2), pool of 6.

02-equation-filled.png
    After tapping the 9. Slot 1 filled; affordance flipped to "operators" — operator
    row bright, number pool dimmed. GDD 3.5's rule that bold-active is always paired
    with dim-inactive, never bold alone.

03-ready-to-commit.png
    9 + 5 staged. "=" is bright with a gold outline; operators now dim, pool bright
    again except the two tiles held in slots. "=" is inert until all three slots fill.

04-won.png
    Cleared. All three targets greyed, pool empty, + budget spent 3 -> 0,
    status CLEARED, target 3/3.

05-failed.png
    Level 1-04, the scripted trap, walked into deliberately. 3 + 1 = 4 is arithmetically
    correct and spends the only +, which also takes a 3 — and target 9 can only be made
    as 3 + 6. Failure fires on the FRONT target being unreachable (GDD 4.1), not on a
    solver verdict about the level: "FAILED - 9 cannot be made from what is left".
    Note fails 1.

06-restarted.png
    After restart. Board back to level start — full pool, budgets restored, target 1/3.
    The failure counter PERSISTS at 1 (GDD 5.1: if it reset, every failure would be free).

07-transform-mode.png
    Level 4-03, sqrt tapped. TRANSFORM MODE: every perfect square in the pool highlights
    (the 4 and the 9) and everything else dims (GDD 3.5). The sqrt button itself shows
    active. Tapping sqrt again cancels. Full 6-target queue visible.

08-transformed.png
    The 9 tapped: rewritten in place to 3, keeping its tile id, marked transformed
    (purple) so it can never be transformed again — no cascading (GDD 3.5). sqrt budget
    spent 1 -> 0. The queue did NOT advance: still target 1/6, tile not consumed.

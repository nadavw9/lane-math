Lane Math — Phase 4B review: assists, modes, disclosure
Taken at PHONE aspect (393x852, GDD §9.1). Phase 3/4A shots were desktop-shaped,
which is how the failure banner ended up below the fold.

Mode is back to Normal by default (Phase 3 brief). Casual is now reachable and
complete: unlimited operators AND the fatal-move warning, per §6.

b1-nothing-unlocked.png
    Fresh save, level 1-01. NOTHING is on screen that has not unlocked (§7.6):
    no star counter, no lives, no hint shop, no mode selector. Absent, not greyed —
    a greyed shop still teaches "this is not for me". Status reads "normal".

b2-scripted-trap.png
    Level 1-4, the scripted trap (§7.5), committing 3 + 1 = 4 — correct arithmetic
    that spends the only + and takes a 3, when target 9 can only be made as 3 + 6.
    The commit is held mid-flight; one line of text, "Wait — what makes the 9?",
    with the real target substituted; the 3 and the 6 pulse in the pool as the only
    two numbers that make it; the move is rewound free — no star, no life, no
    failure (fails 0, target still 1/3).
    Note the mode: NORMAL. 1-4 warns regardless of mode, because it is the teaching
    device. 1-6 repeats the shape with the warning OFF, which is where it is tested.

b3-shop-unlocked.png
    After World 1-3 cleared. Everything now present: lives 5/5, star counter 90★,
    hint shop open with all three types priced, and the mode selector.
    Compare against b1 — same screen, nothing added but progress.

b4-three-hints.png
    All three hints owned on 1-06. None reveals a keystone (§5.4):
      Narrow      "One of the last 2 targets has only one solution." — marks nothing
      Contested   "The 9 is contested." — names the scarce number, outlines the 9s,
                  never says what needs it
      Branch      "Your instinct here is wrong: 5 - 1 loses the level." — kills the
                  tempting fatal option and outlines its tiles, without naming which
                  of the remaining options is correct
    Bought, then failed and restarted: all three are still revealed and re-buying
    charges nothing (starsSpent 12 before and after) — §13, never charge twice for
    the same information on the same level.

b5-casual-warning.png
    Same level 1-06, mode switched to CASUAL, same 5 - 1 move that branch
    elimination named. Casual intercepts it: "Wait — what makes the 17?",
    "5 - 1 loses the level", "got it". Nothing consumed, fails 0, target still 1/3.
    In Normal this move commits and the level is lost two targets later — that is
    the whole difference between the modes, and only disclosure changed.
    Note the plain "got it" here versus 1-4's scripted teaching copy and its
    explicit "rewound free" line.

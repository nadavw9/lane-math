# Phone-eye P1 notes

Rebased onto `origin/master` after PR #7 (automaton motion) + PR #8 (OOL SAFE_TOP clips).

- **PE-05 — `src/renderer/tokens.ts`, `src/renderer/layout.ts`:** Deepened `emptySlot` to named lighter warm-felt recess (`PALETTE.feltRecess`) with stronger brass-lit outer lip; square/circle shape coding kept. Socket, not glow.
- **PE-04 — `src/renderer/renderer.ts`:** OOL concerned automaton seat **128px** (aspect preserved); panel **284px**. Conflict resolve vs #8: kept master’s SAFE_TOP / cartouche / `contentLeft`/`contentTop` interior seating; kept #6 brand-scale seat size.
- **Map focal — `src/map/map-screen.ts`, `src/map/map-plate-presentation.test.ts`:** Durable `mapPlatePresentation` after settle. Open plate 1.10x + cool targetFrontRim + deeper seat; cleared quieter; locked alone uses DIM.
- **PE-02 — `recessedPanel`:** Prefer master’s refined Scout seating (short-glyph X nudge + milder Y lift) from landed P0/#8 stack; do not revert to #6’s coarser lift.
- **Out of scope:** PE-01 desk-shrink rewrite intentionally not included.

## Conflict notes for Scout
- OOL is now **128px seat on #8 chrome** (deeper SAFE_TOP, wait copy in felt, cartouche air). Compare `08-ool-brand.png` vs master’s `20-ool-*.png`: face should read brand-scale, top chrome must stay unclipped.
- Wells + map focal are #6 intent unchanged on current master art stack.

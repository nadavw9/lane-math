node_modules/
dist/
coverage/
*.log
.DS_Store
.vitest/
dist/

# Android signing secrets — never committed.
android/keystore.properties
# Scratch file used only to hand a new keystore password in. Ignored BEFORE it
# is ever created, so the secret cannot be staged even by an `add -A` in the
# window between writing it and deleting it.
android/keystore-new.txt
android/*.keystore
android/*.jks

# Capacitor build output
android/app/build/
android/build/
android/.gradle/

# Synthetic sprite-pipeline fixtures are regenerated, never committed.
assets/sprites-raw/testfamily-*.png
public/assets/sprites/testfamily.*
